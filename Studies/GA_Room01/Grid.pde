class Grid
{
  int[][] gridValue;
  int xNumber;
  int yNumber;
  int unitSize;

  Grid (int x, int y, xNumber, yNumber, unitSize) {
    this.xNumber = xNumber;
    this.yNumber = yNumber;
    this.unitSize = unitSize;

    // Set grid Values
    gridValue = new int[xNumber][yNumber];
    for (int i = 0; i<gridWidth; i++){
      for (int i = 0; i<gridHeight; i++){
        gridValue[i][j] = 0;
      }
    }
  }

  void render(int x, int y, width, height, unitsize){
      //  Create and initialize a boundery grid (set 1 to all grid)

    }
  }